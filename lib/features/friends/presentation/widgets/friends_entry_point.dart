import 'package:flutter/material.dart';

/// Placeholder entry point for the future friends/social feature.
///
/// Not yet wired into the app — kept here so the directory structure is
/// ready for future integration.
class FriendsEntryPoint extends StatelessWidget {
  const FriendsEntryPoint({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
