import 'package:flutter/material.dart';
import '../../timer/presentation/spaceship_timer_screen.dart';

/// Home screen — renders the spaceship Pomodoro timer.
class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SpaceshipTimerScreen();
  }
}
